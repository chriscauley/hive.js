# node related
coverage
.cache
.env.local
dist
node_modules

# python
db.sqlite3
.secret_key
server/settings/local.py
server/settings/.secret_key
__pycache__
.venv
unrest
.static
.media
client/ur
ur

# runtime logs
# daphne writes logs/daphne.log (see deploy/hive.service). logs/.gitkeep is
# tracked so the directory exists in a fresh clone before the service starts --
# systemd's ExecStartPre recreates it too, but only on this host.
logs/*.log
