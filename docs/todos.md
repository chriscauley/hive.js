- make arrows over the pieces showing moves instead of "chalk". When you select a piece it shows the possible moves. Make it so when you hover a destination it shows the pieces the tile moved across.

- add a replay system to go foreward and backwards in the game.

- persist new game settings (AI enabled, difficulty, color, preset) in local storage so they restore when returning to the new game menu. The save/load code is in NewGame.vue using LS_AI_KEY but it's not working.